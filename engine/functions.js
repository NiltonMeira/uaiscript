export const functions = {
    // print
    0: (text) => console.log(text),
    // declarar variavel
    1: (value, identifier) => values[identifier] = value,
    // soma
    2: (a, b) => a + b,
    // subtração 
    3: (a, b) => a - b,
    // multiplicação
    4: (a, b) => a * b,
    // divisão
    5: (a, b) => a / b,
}